apiVersion: v1
kind: Service
metadata:
  name: node-app-service
spec:
  type: LoadBalancer           # Exposes service via cloud provider LB
  selector:
    app: user                   ##very important
  ports:
    - port: 80                # Service port (external clients use this)
      targetPort: 3130        # Container port inside the pod
